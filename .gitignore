node_modules
dist
.DS_Store
*.log

# Secrets: the TMDB token lives here and must never be committed
.env
.env.*
!.env.example

# TMDB data written by `npm run sync`
src/data/

# Local tooling. Only the skills in .claude/skills/ are shared.
.claude/*
!.claude/skills/
